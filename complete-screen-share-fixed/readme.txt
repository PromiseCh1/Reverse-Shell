screenshot – takes one shot and displays it (press any key to close).

stream – starts a live stream updating every ~0.5 seconds. Press q in the OpenCV window to stop.

Any other command (e.g., whoami, dir) – executes on victim.

exit – closes connection.

